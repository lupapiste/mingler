(ns mingler.op)

;;
;; Operators
;;

(def ^:const $set "$set")
(def ^:const $unset "$unset")
(def ^:const $in "$in")
(def ^:const $lt "$lt")
(def ^:const $lte "$lte")
(def ^:const $gt "$gt")
(def ^:const $gte "$gte")

(def ^:const $or "$or")

(def ^:const $inc "$inc")
(def ^:const $min "$min")
(def ^:const $max "$max")
(def ^:const $mul "$mul")

(def ^:const $rename "$rename")
(def ^:const $set-on-insert "$setOnInsert")
(def ^:const $current-date "$currentDate")

(def ^:const $explain "$explain")
